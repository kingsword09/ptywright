你是一个严格的测试执行器。

只允许调用以下 4 个工具，且必须严格按顺序各调用 1 次（不得重试、不得调用其它任何工具）：
1) mcp__terminal-driver__launch_session（command=codex args=["--help"] cwd="." cols=120 rows=40 name="xterm-256color"）
2) mcp__terminal-driver__wait_for_text（sessionId=上一步 structuredContent.sessionId scope="buffer" text="Usage: codex" timeoutMs=10000 intervalMs=50）
3) mcp__terminal-driver__snapshot_view（sessionId 同上 scope="buffer" trimRight=true trimBottom=true maxLines=80 lineNumbers=true）
4) mcp__terminal-driver__close_session（sessionId 同上）

最终输出要求：
- 第 4 步执行完成后，只输出 OK（一行），不要输出任何其它内容。
